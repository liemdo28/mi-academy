# GENERATED FILE — DO NOT EDIT MANUALLY
# Source: contracts/openapi.yaml
# Generated: 2026-07-17
# Owner: Dev 7
openapi: 3.1.0
info:
  title: MI Academy API
  version: "1.0.0"
  description: |
    MI Academy backend API — parent dashboard, child progress, games, and sync.
    All contracts are versioned. Breaking changes increment the major version.
  contact:
    name: MI Academy Platform Team
  license:
    name: Proprietary

servers:
  - url: /api/v1
    description: Current production

security:
  - BearerAuth: []

tags:
  - name: auth
    description: Authentication and token management
  - name: parent
    description: Parent profile management
  - name: children
    description: Child profile management
  - name: lessons
    description: Lesson catalog and content
  - name: games
    description: Game catalog and sessions
  - name: progress
    description: Progress tracking and mastery
  - name: sync
    description: Offline sync
  - name: rewards
    description: Rewards and achievements
  - name: admin
    description: Admin content management

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              example: CONTENT_VERSION_UNSUPPORTED
            message:
              type: string
            requestId:
              type: string
            retryable:
              type: boolean
              default: false
            details:
              type: object
              additionalProperties: true
            schemaVersion:
              type: integer
              default: 1

    RegisterRequest:
      type: object
      required: [email, password, display_name]
      properties:
        email:
          type: string
          format: email
          maxLength: 255
        password:
          type: string
          minLength: 8
          maxLength: 128
        display_name:
          type: string
          minLength: 1
          maxLength: 100
        language:
          type: string
          enum: [vi, en]
          default: vi

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string

    RefreshRequest:
      type: object
      required: [refresh_token]
      properties:
        refresh_token:
          type: string

    TokenResponse:
      type: object
      required: [access_token, refresh_token, token_type]
      properties:
        access_token:
          type: string
        refresh_token:
          type: string
        token_type:
          type: string
          enum: [bearer]
          default: bearer

    AuthResponse:
      type: object
      required: [user, access_token, refresh_token]
      properties:
        user:
          $ref: "#/components/schemas/UserResponse"
        parent_profile:
          $ref: "#/components/schemas/ParentProfileResponse"
        access_token:
          type: string
        refresh_token:
          type: string

    UserResponse:
      type: object
      required: [id, role, email, created_at]
      properties:
        id:
          type: string
          format: uuid
        role:
          type: string
          enum: [parent, admin]
        email:
          type: string
          format: email
        created_at:
          type: string
          format: date-time

    ParentProfileResponse:
      type: object
      required: [id, display_name, language, timezone, has_pin, created_at]
      properties:
        id:
          type: string
          format: uuid
        display_name:
          type: string
        language:
          type: string
          enum: [vi, en]
        timezone:
          type: string
        has_pin:
          type: boolean
        created_at:
          type: string
          format: date-time
        children:
          type: array
          items:
            $ref: "#/components/schemas/ChildResponse"

    ParentProfileUpdate:
      type: object
      properties:
        display_name:
          type: string
          minLength: 1
          maxLength: 100

    SetPinRequest:
      type: object
      required: [pin]
      properties:
        pin:
          type: string
          pattern: "^[0-9]{4,6}$"

    VerifyPinRequest:
      type: object
      required: [pin]
      properties:
        pin:
          type: string
          pattern: "^[0-9]{4,6}$"

    VerifyPinResponse:
      type: object
      required: [verified]
      properties:
        verified:
          type: boolean

    ChildResponse:
      type: object
      required: [id, nickname, age_group, preferred_language, created_at]
      properties:
        id:
          type: string
          format: uuid
        nickname:
          type: string
          maxLength: 50
        birth_year:
          type: integer
        age_group:
          $ref: "#/components/schemas/AgeGroup"
        grade_level:
          type: string
        avatar_id:
          type: string
        preferred_language:
          type: string
          enum: [vi, en]
        daily_time_limit_minutes:
          type: integer
        created_at:
          type: string
          format: date-time

    CreateChildRequest:
      type: object
      required: [nickname, age_group, preferred_language]
      properties:
        nickname:
          type: string
          minLength: 1
          maxLength: 50
        birth_year:
          type: integer
        age_group:
          $ref: "#/components/schemas/AgeGroup"
        grade_level:
          type: string
        avatar_id:
          type: string
          default: avatar_default
        preferred_language:
          type: string
          enum: [vi, en]
          default: vi
        daily_time_limit_minutes:
          type: integer

    UpdateChildRequest:
      type: object
      properties:
        nickname:
          type: string
          minLength: 1
          maxLength: 50
        avatar_id:
          type: string
        preferred_language:
          type: string
          enum: [vi, en]
        daily_time_limit_minutes:
          type: integer

    AgeGroup:
      type: string
      enum: [junior, explorer, master]

    AccessibilityPreferencesSchema:
      type: object
      properties:
        high_contrast:
          type: boolean
          default: false
        large_text:
          type: boolean
          default: false
        reduce_motion:
          type: boolean
          default: false
        screen_reader:
          type: boolean
          default: false
        font_size:
          type: number
          minimum: 0.8
          maximum: 2.0
          default: 1.0

    AudioPreferencesSchema:
      type: object
      properties:
        music_volume:
          type: number
          minimum: 0.0
          maximum: 1.0
          default: 0.8
        sfx_volume:
          type: number
          minimum: 0.0
          maximum: 1.0
          default: 1.0
        speech_enabled:
          type: boolean
          default: true

    LessonResponse:
      type: object
      required: [id, title, subject_id, age_group, difficulty, estimated_minutes, language, is_active]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: object
          additionalProperties:
            type: string
        description:
          type: string
        subject_id:
          type: string
        age_group:
          $ref: "#/components/schemas/AgeGroup"
        difficulty:
          type: integer
          minimum: 1
          maximum: 5
        language:
          type: string
          enum: [vi, en]
        estimated_minutes:
          type: integer
        content_version:
          type: integer
          default: 1
        is_active:
          type: boolean

    LessonListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/LessonResponse"
        total:
          type: integer

    GameLevelResponse:
      type: object
      required: [id]
      properties:
        id:
          type: string
          format: uuid
        game_id:
          type: string
          format: uuid
        level_number:
          type: integer
          minimum: 1
        difficulty:
          type: integer
          minimum: 1
          maximum: 5

    GameListItem:
      type: object
      required: [id, name, game_type, is_active]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        game_type:
          type: string
          enum: [memory, word_builder, sound_match, math_race, math_supermarket, robot_commands]
        age_min:
          type: integer
        age_max:
          type: integer
        is_active:
          type: boolean

    GameDetail:
      type: object
      required: [id, name, game_type, is_active]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        game_type:
          type: string
          enum: [memory, word_builder, sound_match, math_race, math_supermarket, robot_commands]
        age_min:
          type: integer
        age_max:
          type: integer
        config_json:
          type: object
          additionalProperties: true
        is_active:
          type: boolean

    GameStartRequest:
      type: object
      required: [child_id]
      properties:
        child_id:
          type: string
          format: uuid

    GameAttemptRequest:
      type: object
      required: [child_id, answer_json, response_time_ms, hint_count]
      properties:
        child_id:
          type: string
          format: uuid
        answer_json:
          type: object
          additionalProperties: true
        response_time_ms:
          type: integer
        hint_count:
          type: integer
          minimum: 0

    GameCompleteRequest:
      type: object
      required: [child_id, total_stars]
      properties:
        child_id:
          type: string
          format: uuid
        total_stars:
          type: integer
          minimum: 0
          maximum: 3
        badges_unlocked:
          type: array
          items:
            type: string
          default: []
        rewards_unlocked:
          type: array
          items:
            type: string
          default: []

    MiGameLaunchRequest:
      type: object
      description: Full game launch request from platform to game. NEVER contains tokens or PII.
      required: [schema_version, child_profile_id, game_id, level_id, language, age_group, accessibility, audio_preferences, level_content]
      properties:
        schema_version:
          type: integer
          default: 1
        child_profile_id:
          type: string
          format: uuid
        game_id:
          type: string
          format: uuid
        level_id:
          type: string
          format: uuid
        language:
          type: string
          enum: [vi, en]
        age_group:
          $ref: "#/components/schemas/AgeGroup"
        accessibility:
          $ref: "#/components/schemas/AccessibilityPreferencesSchema"
        audio_preferences:
          $ref: "#/components/schemas/AudioPreferencesSchema"
        level_content:
          type: object
          additionalProperties: true
        restored_state:
          type: object
          additionalProperties: true

    MiGameResult:
      type: object
      description: Game result from game to platform. NEVER contains tokens or PII.
      required: [schema_version, attempt_id, child_profile_id, game_id, level_id, started_at, completed_at, attempt_count, correct_count, incorrect_count, hint_count, duration_seconds, completed, mastery_evidence, skill_evidence]
      properties:
        schema_version:
          type: integer
          default: 1
        attempt_id:
          type: string
          format: uuid
        child_profile_id:
          type: string
          format: uuid
        game_id:
          type: string
          format: uuid
        level_id:
          type: string
          format: uuid
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
        attempt_count:
          type: integer
          minimum: 0
        correct_count:
          type: integer
          minimum: 0
        incorrect_count:
          type: integer
          minimum: 0
        hint_count:
          type: integer
          minimum: 0
        duration_seconds:
          type: integer
          minimum: 0
        completed:
          type: boolean
        mastery_evidence:
          type: number
          minimum: 0.0
          maximum: 1.0
        skill_evidence:
          type: object
          additionalProperties:
            type: number
        metadata:
          type: object
          additionalProperties: true
          default: {}

    MiGameSnapshot:
      type: object
      description: Game snapshot for save/resume. NEVER contains tokens or PII.
      required: [schema_version, game_version, game_id, level_id, child_profile_id, saved_at, state]
      properties:
        schema_version:
          type: integer
          default: 2
        game_version:
          type: string
          default: "1.0.0"
        game_id:
          type: string
          format: uuid
        level_id:
          type: string
          format: uuid
        child_profile_id:
          type: string
          format: uuid
        saved_at:
          type: string
          format: date-time
        state:
          type: object
          additionalProperties: true
        checksum:
          type: string
          description: Optional deterministic integrity checksum over the stable snapshot payload.

    LessonStatusEnum:
      type: string
      enum: [notStarted, learning, completed, needsPractice, mastered]

    ProgressResponse:
      type: object
      required: [child_id, lesson_id, status, mastery_score, total_attempts]
      properties:
        child_id:
          type: string
          format: uuid
        lesson_id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/LessonStatusEnum"
        mastery_score:
          type: number
          minimum: 0.0
          maximum: 1.0
        total_attempts:
          type: integer
          minimum: 0
        last_played_at:
          type: string
          format: date-time

    SkillReport:
      type: object
      required: [skill_id, name, mastery_score]
      properties:
        skill_id:
          type: string
        name:
          type: string
        mastery_score:
          type: number
          minimum: 0.0
          maximum: 1.0

    DailyPlanItem:
      type: object
      required: [lesson_id, title, reason, priority]
      properties:
        lesson_id:
          type: string
          format: uuid
        title:
          type: string
        reason:
          type: string
        priority:
          type: integer
          minimum: 1
          maximum: 10

    SyncProgressItem:
      type: object
      required: [lesson_id, status, mastery_score]
      properties:
        lesson_id:
          type: string
          format: uuid
        status:
          type: string
        mastery_score:
          type: number

    SyncAttemptItem:
      type: object
      required: [lesson_id, game_id, answer_json, is_correct, response_time_ms]
      properties:
        lesson_id:
          type: string
        game_id:
          type: string
        question_id:
          type: string
        answer_json:
          type: object
        is_correct:
          type: boolean
        response_time_ms:
          type: integer
        hint_count:
          type: integer

    SyncSessionItem:
      type: object
      required: [child_id, started_at, ended_at, duration_seconds]
      properties:
        child_id:
          type: string
        started_at:
          type: string
          format: date-time
        ended_at:
          type: string
          format: date-time
        duration_seconds:
          type: integer

    SyncContentResponse:
      type: object
      required: [changes]
      properties:
        content_version:
          type: integer
        changes:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
                enum: [add, update, delete]
              lesson_id:
                type: string
              version:
                type: integer

    SyncStatusResponse:
      type: object
      required: [content_version, progress_version]
      properties:
        content_version:
          type: integer
        progress_version:
          type: integer
        last_sync_at:
          type: string
          format: date-time

    RewardResponse:
      type: object
      required: [id, type, name]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          enum: [star, badge, sticker, avatarItem]
        name:
          type: string
        description:
          type: string
        asset_url:
          type: string

    ChildRewardResponse:
      type: object
      required: [id, reward, unlocked_at]
      properties:
        id:
          type: string
          format: uuid
        reward:
          $ref: "#/components/schemas/RewardResponse"
        unlocked_at:
          type: string
          format: date-time

    PaginatedResponse:
      type: object
      required: [items, total, page, page_size]
      properties:
        items:
          type: array
        total:
          type: integer
        page:
          type: integer
        page_size:
          type: integer

paths:
  /auth/register:
    post:
      tags: [auth]
      summary: Register new parent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: User created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "409":
          description: Email already registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /auth/login:
    post:
      tags: [auth]
      summary: Login parent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /auth/refresh:
    post:
      tags: [auth]
      summary: Refresh access token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshRequest"
      responses:
        "200":
          description: Token refreshed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenResponse"

  /auth/me:
    get:
      tags: [auth]
      summary: Get current user
      responses:
        "200":
          description: Current user info
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserResponse"

  /parent/profile:
    get:
      tags: [parent]
      summary: Get parent profile
      responses:
        "200":
          description: Parent profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParentProfileResponse"
    put:
      tags: [parent]
      summary: Update parent profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ParentProfileUpdate"
      responses:
        "200":
          description: Profile updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ParentProfileResponse"

  /parent/pin:
    post:
      tags: [parent]
      summary: Set parent PIN
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetPinRequest"
      responses:
        "200":
          description: PIN set

  /parent/pin/verify:
    post:
      tags: [parent]
      summary: Verify parent PIN
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyPinRequest"
      responses:
        "200":
          description: PIN verified
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerifyPinResponse"

  /children:
    get:
      tags: [children]
      summary: List children
      responses:
        "200":
          description: List of children
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChildResponse"
    post:
      tags: [children]
      summary: Create child profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChildRequest"
      responses:
        "201":
          description: Child created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildResponse"

  /children/{child_id}:
    get:
      tags: [children]
      summary: Get child details
      parameters:
        - name: child_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Child details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildResponse"
    put:
      tags: [children]
      summary: Update child profile
      parameters:
        - name: child_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChildRequest"
      responses:
        "200":
          description: Child updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChildResponse"

  /lessons:
    get:
      tags: [lessons]
      summary: List lessons
      parameters:
        - name: age_group
          in: query
          schema:
            $ref: "#/components/schemas/AgeGroup"
        - name: subject_id
          in: query
          schema:
            type: string
        - name: language
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Lesson list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonListResponse"

  /lessons/{lesson_id}:
    get:
      tags: [lessons]
      summary: Get lesson details
      parameters:
        - name: lesson_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Lesson details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonResponse"

  /games:
    get:
      tags: [games]
      summary: List games
      parameters:
        - name: age_group
          in: query
          schema:
            $ref: "#/components/schemas/AgeGroup"
      responses:
        "200":
          description: Game list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GameListItem"

  /games/{game_id}:
    get:
      tags: [games]
      summary: Get game details
      parameters:
        - name: game_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Game detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GameDetail"

  /games/{game_id}/start:
    post:
      tags: [games]
      summary: Start game session
      parameters:
        - name: game_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GameStartRequest"
      responses:
        "200":
          description: Game session started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MiGameLaunchRequest"

  /games/{game_id}/attempt:
    post:
      tags: [games]
      summary: Submit game attempt
      parameters:
        - name: game_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GameAttemptRequest"
      responses:
        "200":
          description: Attempt recorded

  /games/{game_id}/complete:
    post:
      tags: [games]
      summary: Complete game session
      parameters:
        - name: game_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GameCompleteRequest"
      responses:
        "200":
          description: Game completed

  /progress/children/{child_id}:
    get:
      tags: [progress]
      summary: Get child progress
      parameters:
        - name: child_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Progress data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProgressResponse"

  /progress/children/{child_id}/skills:
    get:
      tags: [progress]
      summary: Get child skill report
      parameters:
        - name: child_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Skill reports
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SkillReport"

  /progress/children/{child_id}/daily-plan:
    get:
      tags: [progress]
      summary: Get daily learning plan
      parameters:
        - name: child_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Daily plan
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DailyPlanItem"

  /sync/status:
    get:
      tags: [sync]
      summary: Get sync status
      responses:
        "200":
          description: Sync status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncStatusResponse"

  /sync/content:
    get:
      tags: [sync]
      summary: Get content delta since last sync
      parameters:
        - name: since_version
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Content changes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncContentResponse"

  /sync/progress:
    post:
      tags: [sync]
      summary: Upsert progress from client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  type: array
                  items:
                    $ref: "#/components/schemas/SyncProgressItem"
      responses:
        "200":
          description: Progress synced

  /sync/attempts:
    post:
      tags: [sync]
      summary: Upload game attempts
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  type: array
                  items:
                    $ref: "#/components/schemas/SyncAttemptItem"
      responses:
        "200":
          description: Attempts synced

  /sync/sessions:
    post:
      tags: [sync]
      summary: Upload session data
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  type: array
                  items:
                    $ref: "#/components/schemas/SyncSessionItem"
      responses:
        "200":
          description: Sessions synced

  /rewards/children/{child_id}:
    get:
      tags: [rewards]
      summary: Get child rewards
      parameters:
        - name: child_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Child rewards
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChildRewardResponse"
