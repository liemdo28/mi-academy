name: content_sdk
description: Content management SDK for MI Academy — lessons, levels, curriculum, and localization.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_models:
    path: ../shared_models
  http: ^1.1.0
  json_annotation: ^4.8.0
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
