name: mi_game_accessibility
description: Accessibility utilities for MI Academy games — text scaling, screen reader, reduced motion, tap alternatives.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  mi_game_core:
    path: ../mi_game_core
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
