name: adaptive_sdk
description: Adaptive Learning SDK for MI Academy — difficulty adjustment, spaced repetition, and personalization contracts.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  json_annotation: ^4.8.1
  uuid: ^4.2.1
  collection: ^1.17.0
  math: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  mockito: ^5.4.0
