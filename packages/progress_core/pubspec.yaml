name: progress_core
description: MI Academy progress tracking, mastery calculation, and adaptive learning
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
