name: game_sdk
description: Game lifecycle SDK for MI Academy — launch, snapshot, result, and resume contracts.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_models:
    path: ../shared_models
  http: ^1.1.0
  json_annotation: ^4.8.0
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
